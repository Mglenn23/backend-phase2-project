'use strict'

// Require everything here, inject them where necessary.
const run = require('./lib/run')
const version = require('./lib/version')
const command = require('./lib/command')
const formatOptions = require('./lib/format')
const gitSomeModule = require('./lib/index')

// Inject & export
module.exports = gitSomeModule({
  run,
  version,
  command,
  formatOptions
})
