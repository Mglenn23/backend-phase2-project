'use strict'

const gs = require('./index')({ path: '.', tag: true })

const keywords = {
  minor: ['merge', 'feature'],
  patch: ['patch', 'fix', 'refactor', 'tweak', 'update', 'add']
}

console.log(gs.version(keywords).next)
